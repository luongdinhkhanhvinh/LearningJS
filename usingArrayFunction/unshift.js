// 3 unshift
let actor = ["User", "Driver", "Deliver", "Customer", "Admin"];

// Inserts new elements at the start of an array, and returns the new length of the array.
actor.unshift("Manager");

console.log("actor", actor);
